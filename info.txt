2025-08-08

Visually replicates the analytical solution with expected error. 
---

Modifications since last version:
- Deleted fvm Python code

- Added position variable to help define the bed geometry correctly

- Defined dx with reference to the number of nodes

- Shifted the cells by dx/2 to allow the centred force scheme to be used

- Added hCentered variable which stores h values and interpolated h values for centred force scheme

- Allocated arrays used in body force definition (H_part, zb, dzbdzx, hCentered, force_x, force_y) with dimensions 2*Lx+1,2*Ly+1 instead of Lx,Ly

- Created update_body_force subroutine to calculate the current body force at each time step and no only the initial one

- Used centered body force scheme in collide_stream subroutine

- Modified the way the positions are written in write_csv

- Defined the unknown macroscopic values at inflow and outflow using consistency check equation

---

Potential next step(s):

- Adapt the code to run the next case: Flow Around a Cylinder