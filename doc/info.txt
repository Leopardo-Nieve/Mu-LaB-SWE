2026-08-13

Body force scheme seems to be the culprit of low order of convergence per literature review. However, to make the code work dynamically and save time in the future, a case_parameter file will be used to define the case while the code should remain untouched between cases.
This will allow domain size, force scheme choice etc. to be selected easily.

---

Modifications since last version:
- Created Makefile that works with codeblocks
- Created a configuration file called parameters.cfg to work with config_fortran
- Modified main.f90 to work with parameters.cfg

---

Potential next step(s):
- create a source term variable that is defined differently based on the force scheme chosen
